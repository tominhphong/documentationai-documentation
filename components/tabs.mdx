---
title: Tabs
description: "Component Tabs — tổ chức các variant nội dung như languages, platforms, code examples, hoặc workflow alternatives trên cùng một page."
last_reviewed: 2026-04-22
keywords: [mdx components, mintlify, documentation, ui patterns, phong to realtor, exp realty, dfw, sherman tx]
---

# Tabs

Tabs group related variants like language or platform.

## Example

<Tabs>
  <Tab title="macOS" icon="apple">
    Install using Homebrew for the best experience on macOS.

    ```bash
    brew install documentation-ai
    ```
  </Tab>
  <Tab title="Windows" icon="monitor">
    Download and run the Windows installer from the releases page.

    ```bash
    winget install documentation-ai
    ```
  </Tab>
  <Tab title="Linux" icon="terminal">
    Use your package manager or download the binary directly.

    ```bash
    sudo apt install documentation-ai
    ```
  </Tab>
</Tabs>

## Learn more

Read the full Tabs guide at https://documentation.ai/docs/components/tabs.
